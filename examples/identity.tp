;; Example showing variable binding
(require prelude/typeclass/classes)
(provide identity)

(let identity
    (lambda (x) x))

(let double_identity
    ((identity identity) 5))

(+ 2 double_identity)
